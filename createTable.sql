CREATE TABLE IF NOT EXISTS {} (Name VARCHAR(255) NOT NULL PRIMARY KEY,
                               Cust_I VARCHAR(18) NOT NULL,
                               Open_Dt DATE NOT NULL,
                               Consul_Dt DATE,
                               VAC_ID CHAR(5),
                               DR_Name VARCHAR(255), 
                               State CHAR(5), 
                               Country CHAR(5),
                               DOB DATE,
                               FLAG CHAR(1));

